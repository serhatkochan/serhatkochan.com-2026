---
import { navigation, site } from '../../data/site';
import OptimizedImage from '../ui/OptimizedImage.astro';
import avatar from '../../assets/avatar.png';
import Container from './Container.astro';
import ThemeToggle from '../ui/ThemeToggle.astro';
import MobileNav from '../../islands/MobileNav';
import { navItemIsActive } from '../../lib/nav';
import { buildCanonical } from '../../lib/seo';

const currentPath = Astro.url.pathname.replace(/\/$/, '') || '/';
const items = navigation.map((item) => ({ ...item, href: buildCanonical(item.href) }));
---

<header class="relative z-50 pt-6">
  <Container>
    <div class="flex items-center gap-4">
      <div class="flex flex-1">
        <a href={buildCanonical('/')} class="group block cursor-pointer" aria-label={`${site.name} anasayfa`}>
          <OptimizedImage
            src={avatar}
            alt=""
            width={44}
            height={44}
            loading="eager"
            sizes="44px"
            class="rounded-full object-cover shadow-sm ring-2 ring-zinc-200 transition group-hover:ring-primary/40 dark:ring-zinc-700 dark:group-hover:ring-primary/50"
          />
        </a>
      </div>

      <nav class="hidden flex-1 justify-center md:flex" aria-label="Ana menü">
        <ul class="flex rounded-full bg-white/90 px-3 text-sm font-medium text-zinc-800 shadow-lg shadow-zinc-800/5 ring-1 ring-zinc-900/5 backdrop-blur dark:bg-zinc-800/90 dark:text-zinc-200 dark:ring-white/10">
          {
            navigation.map((item) => (
              <li>
                <a
                  href={buildCanonical(item.href)}
                  class:list={[
                    'block px-3 py-2 transition',
                    navItemIsActive(item.href, currentPath)
                      ? 'text-primary-dark dark:text-primary-light'
                      : 'hover:text-primary-dark dark:hover:text-primary-light',
                  ]}
                  aria-current={navItemIsActive(item.href, currentPath) ? 'page' : undefined}
                >
                  {item.name}
                </a>
              </li>
            ))
          }
        </ul>
      </nav>

      <div class="flex flex-1 items-center justify-end gap-3">
        <MobileNav client:media="(max-width: 767px)" items={items} currentPath={currentPath} />
        <ThemeToggle />
      </div>
    </div>
  </Container>
</header>
